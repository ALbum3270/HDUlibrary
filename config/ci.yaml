# GitHub Actions configuration. Credentials come from repository Secrets and
# must not be written in this file.
user:
  login_name: ""
  password: ""
  org_id: "104"

# First configure a real plan locally with the GUI, then copy its values here.
plans:
  - id: "example_plan"
    room_name: "替换为实际房间名"
    floor_name: "替换为实际楼层名"
    begin_time: "08:00:00"
    duration_hours: 4
    seats:
      - seat_id: "替换为实际座位ID"
        seat_num: "替换为显示座位号"

# Keep enabled=false until every placeholder above has been replaced.
# target_weekdays uses 1=Monday through 7=Sunday for the target seat date.
schedules:
  - mode: weekdays
    target_weekdays: [1, 2, 3, 4, 5, 6, 7]
    plan_ids: ["example_plan"]
    enabled: false

settings:
  interval: 5
  max_try_times: 60
  auto_relogin: true
  booking_open_time: "20:00:00"

api:
  base_url: "https://hdu.huitu.zhishulib.com"

session:
  headers:
    Accept: "*/*"
    Accept-Encoding: "gzip, deflate"
    Accept-Language: "zh-CN,zh;q=0.9"
    Cache-Control: "no-cache"
    Connection: "keep-alive"
    Pragma: "no-cache"
    Referer: "https://hdu.huitu.zhishulib.com/"
    Sec-Fetch-Dest: "empty"
    Sec-Fetch-Mode: "no-cors"
    Sec-Fetch-Site: "same-origin"
    User-Agent: "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/124 Safari/537.36"
  params:
    LAB_JSON: "1"
  trust_env: false
  verify: false
